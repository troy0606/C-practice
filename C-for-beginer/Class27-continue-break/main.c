#include <stdio.h>

int main(void)
{

  /**
   * continue break 輔助控制語句
   *
   * continue: 只能用在循環語句，用於跳過本次循環 continue 後的內容
   * break: 可以用在 switch 跟循環語句，在循環語句內用於 跳出整個循環語句
   *
   * ex:
   * while(condition) {
   *    continue
   *    ---跳過之後的內容，繼續下個循環
   * }
   *
   * for(inital state; condition ; change state) {
   *    continue
   *    ---跳過之後的內容，繼續下個循環
   * }
   *
   * while(condition) {
   *    break;
   *    ---跳出這循環語句
   * }
   *
   * for(inital state; condition ; change state) {
   *    break;
   *    ---跳出這循環語句
   * }
   */

  return 0;
}
